[deploy]
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 600
startCommand = "sleep 5 && /app/.venv/bin/streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true --browser.gatherUsageStats=false"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
